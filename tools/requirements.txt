# ESP32 LoRa Sniffer - Python Tools Requirements

# Flashing pre-built binaries (flash.bat / flash.sh)
esptool>=4.0.0

# Core — map.py, topology.py
matplotlib>=3.5.0
networkx>=3.0

# map.py — interactive HTML maps (recommended)
folium>=0.14.0

# report.py, monitor.py, api_client.py — live API / WebSocket access
requests>=2.28.0
websocket-client>=1.4.0

# monitor.py — live TUI (top talkers, alerts, decrypt feed)
rich>=13.0.0

# report.py, topology.py, monitor.py — PSK decryption
cryptography>=41.0.0

# Optional analysis modules (meshtastic_decoder.py, meshcore_decoder.py)
# pandas>=1.3.0
# numpy>=1.21.0
